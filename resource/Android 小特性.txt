2015.9.11  修复 华为P8 5.0机型银联掌柜对账交易明细item无文字；
textview 默认颜色变化，5.0与低版本不一致
案例1  银联掌柜收款对账交易明细查询里  listView  background nocolor ，item默认颜色与背景色一致。需设置TextColor


2015.8
调用选择照片服务时
intent.setAction(Intent.ACTION_GET_CONTENT);从文件列表选择，部分华为机型获取照片的URI地址无效
换成
intent.setAction(Intent.ACTION_PICK);


activity 配置  android:launchMode="singleInstance" 时，
startActivityForResult 会被即时调用 onAcitivityResult。

被调用时调用的activity也会即时被调用onAcitivityResult。



（
父类

Activity a = this;

子类
a.startActivityForResult

）

与 

this.startActivityForResult

存在差异